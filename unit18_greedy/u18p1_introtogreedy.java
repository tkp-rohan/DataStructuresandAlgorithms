package unit18_greedy;

// Greedy Algorithms

// General Structure
// getOptimal(Item arr[], int n){
    // 1) Initialize : res = 0
    // 2) while (All items are not considered)
    // {
    //      i = selectAnItem()
    //      if (feasible(i))
    //          res = res + i
    // }
    // 3) Return res
// }

public class u18p1_introtogreedy {
    
}
